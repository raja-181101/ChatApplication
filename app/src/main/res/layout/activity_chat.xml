<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:fitsSystemWindows="true">

    <!-- Top bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/background"
        android:padding="@dimen/extra_small_padding"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/chattingStatusText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/chatting_with_stranger"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_size_small"
            android:textStyle="bold"/>


        <com.google.android.material.button.MaterialButton
            android:id="@+id/leaveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/leave_button"
            android:padding="@dimen/extra_small_padding"
            android:text="@string/leave_btn"
            android:textColor="@color/leave_button_text"
            android:textSize="@dimen/text_size_semi_small" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/ReportButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/extra_small_margin"
            android:backgroundTint="@color/report_button"
            android:padding="@dimen/extra_small_padding"
            android:text="@string/report_btn"
            android:textColor="@color/report_button_text"
            android:textSize="@dimen/text_size_semi_small" />

    </LinearLayout>

    <!-- Messages list -->

    <!-- Message input bar at bottom -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="@dimen/extra_small_padding" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/background"
        android:padding="@dimen/extra_small_padding"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/messageInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/input_background"
            android:hint="@string/type_message_hint"
            android:importantForAutofill="no"
            android:inputType="textMultiLine"
            android:maxLines="3"
            android:padding="@dimen/input_field_padding"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/input_hint"
            tools:ignore="VisualLintTextFieldSize" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/sendButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/extra_small_margin"
            android:backgroundTint="@color/send_button"
            android:padding="@dimen/input_field_padding"
            android:text="@string/send_btn"
            android:textColor="@color/send_button_text" />
    </LinearLayout>

</LinearLayout>
